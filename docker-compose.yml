version: '3'

services:
  mysql:
    image: mysql:latest
    volumes:
      - .db_data:/var/lib/mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpw
      MYSQL_DATABASE: reqbaz
      MYSQL_USER: reqbaz
      MYSQL_PASSWORD: password
    ports:
      - 3306:3306
